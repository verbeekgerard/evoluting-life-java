package eu.luminis.geometry;

/**
 * A vector that represents a velocity
 */
public class Velocity extends Vector{
    public Velocity(double angle, double length) {
        super(angle, length, true);
    }
}
